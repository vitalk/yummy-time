import { moduleFor, test } from 'ember-qunit';

moduleFor('controller:order/notify', 'Unit | Controller | order/notify', {
});

test('it exists', function(assert) {
  let controller = this.subject();
  assert.ok(controller);
});
