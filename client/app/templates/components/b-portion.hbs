<div class="b-portion__row">

  {{#if canDelete}}
    <button class="i-button-remove b-portion__remove-btn" {{action 'remove' portion}}></button>
  {{else}}
    <button class="i-button-remove i-button-remove_disabled b-portion__remove-btn"></button>
  {{/if}}
  <div class="b-portion__text">
    {{#if portion.product}}
      {{portion.product.name}}
    {{else}}
      {{portion.text}}
    {{/if}}
  </div>
  <div class="b-portion__cost">{{format-currency portion.cost}}</div>
  {{b-checkbox checked=portion.paid disabled=isManager
               onchange=(action 'togglePaid' portion)}}
</div>
