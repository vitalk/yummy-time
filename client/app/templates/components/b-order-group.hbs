<div class="b-order-group__wrapper">
  <div class="b-order-group__row">

    {{#if canDelete}}
      <button {{action 'removeAll'}} class="i-button-remove b-portion__remove-btn"></button>
    {{else}}
      <button class="i-button-remove i-button-remove_disabled b-portion__remove-btn"></button>
    {{/if}}
    <div class="b-order-group__person b-person">
      <span class="b-person__name">{{owner.displayName}}</span>
      <span class="b-person__phone">{{owner.phone}}</span>
    </div>

    <div class="b-order-group__money b-order-money">
      <span class="b-order-money__available">{{format-currency moneyAvailable}}</span>
      <span class="b-order-money__total">{{format-currency moneyTotal}}</span>
    </div>

    {{b-checkbox class="b-order-group__checkbox"
                 checked=allPaid disabled=canCheck
                 onchange=(action 'toggleAll') }}
  </div>
</div>

<ul class="b-list b-order-group__summary">
  {{#each portions as |portion|}}
    {{b-portion portion=portion canCheck=canCheck canDelete=canDelete}}
  {{/each}}
</ul>
